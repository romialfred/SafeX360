# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
playwright-report/
test-results/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Capacitor / Android (SafeX 360 Field) ────────────────────────────
# Le dossier android/ est genere par `npx cap add android` au build.
# On le regenere a la demande (CI ou dev), pas besoin de le versionner.
android/

# Keystore et credentials (NE JAMAIS COMMITER)
*.keystore
*.jks
keystore.properties
key.properties
google-services.json

# Fichiers d'environnement — jamais dans un dépôt public.
# (.env.example reste versionné comme modèle.)
.env
.env.*
!.env.example

# Caches Capacitor
.capacitor/

# APK / AAB de sortie
*.apk
*.aab
# Exception : APK distribue via la page login (Vercel)
!public/downloads/SafexMobile.apk
