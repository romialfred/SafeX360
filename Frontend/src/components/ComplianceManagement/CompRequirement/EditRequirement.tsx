import RequirementForm from "./RequirementForm";

/** Modification d'une exigence réglementaire — voir RequirementForm (LOT 49). */
const EditRequirement = () => <RequirementForm mode="edit" />;

export default EditRequirement;
