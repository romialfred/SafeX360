import RequirementForm from "./RequirementForm";

/** Création d'une exigence réglementaire — voir RequirementForm (LOT 49). */
const AddRequirement = () => <RequirementForm mode="create" />;

export default AddRequirement;
